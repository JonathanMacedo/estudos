// Watch = compila o .ts toda vez que salvar o arquivo.

// Declarar a variável para ser realmente uma linguagem fortemente tipada.
let condicao: boolean;
let nome: string;
let idade: number; // int, float, bin, hex

condicao = true;
nome = "Jonathan";
idade = 21;

console.log("Nome: " + nome + " idade: " + idade);
